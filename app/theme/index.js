import { DefaultTheme } from 'react-native-paper'

const theme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    primary: '#FF9800',
    accent: '#8BC34A',
    text: '#212121',
  },
}

export default theme
